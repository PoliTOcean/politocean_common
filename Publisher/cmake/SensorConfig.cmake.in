get_filename_component(Publisher_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${Publisher_CMAKE_DIR})
list(REMOVE_AT CMAKE_MODULE_PATH -1)

if(NOT TARGET JSONUtils::JSONUtils)
    include("${Publisher_CMAKE_DIR}/PublisherTargets.cmake")
endif()

set(Publisher_LIBRARIES Publisher::Publisher)
